UI 只调用这里的 Use Case。application 读仓库、调领域命令、复制媒体文件，再把已读取的数据交给 projection。最多三张图、最多一段音频在这一层强制。回看按发生时间范围计数或分页，不一次加载全部 Moment 和 Asset。权限拒绝、磁盘不足、复制失败和仓库写入失败会留下可恢复草稿；重试不得重复创建 Moment 或 Asset。缺失、打不开的照片与无法播放的声音分开说明，不把媒体错误当成 Moment 不存在。当时的感受写入既有 `content.emotion`；未选择不阻塞保存，未知旧值由 ViewModel 原样展示。家庭身份与成员走 `family-use-cases`，只在真实 API 成功后给出成员；失败或服务端不可达不改个人 Moment，也不展示成员。待确认写操作必须由调用方注入 `pending`；iOS 组合入口 `createIosFamilyUseCases` / `getFamilyUseCases` 注入 SQLite `family_pending_operations` 与 Keychain 会话。家庭页只在真实成员确认后展示成员；未配置或不可达只显示不可用，不编造成员。
